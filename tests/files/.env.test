# ============================================================
# Bodhi — Test environment credentials
# Committed to the repo. Used by pytest when .env is absent.
# Start the stack for testing with:
#   docker compose --env-file .env.test up -d
# ============================================================
POSTGRES_PASSWORD=bodhi_test
NEO4J_PASSWORD=bodhi_test
GRAFANA_PASSWORD=bodhi_test
ARCH=amd64
